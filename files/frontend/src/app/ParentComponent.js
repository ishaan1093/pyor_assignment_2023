// ParentComponent.js

// Add the "use client" directive at the top of the file
// to mark it as a client entry
// use client

import React from 'react';
import Page from './page';

const ParentComponent = () => {
  return (
    <div>
      <Page />
    </div>
  );
};

export default ParentComponent;
